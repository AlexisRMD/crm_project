<%= form_with(model: product) do |form| %>
  <% if product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row mb-3 justify-content-center">
    <%= form.label :name, "Nom", class: "col-auto col-form-label" %>
    <div class="col">
      <%= form.text_field :name, class: "form-control" %>
    </div>
  </div>

 <div class="row mb-3 justify-content-center">
    <%= form.label :category_id, "Catégorie", class: "col-auto col-form-label" %>
    <div class="col">
      <%= form.select :category_id, options_for_select(Category.all.map{|c|[c.name, c.id]}), {}, class: "form-select" %>
    </div>
    <%= link_to(categories_path, class: "btn btn-primary col-auto") do %>
    <i class="bi bi-plus-slash-minus"></i>
    <% end %>
  </div>

 <div class="row mb-3 justify-content-center">
    <%= form.label :price, "Prix", class: "col-auto col-form-label" %>
    <div class="col">
      <%= form.text_field :price, class: "form-control" %>
    </div>
  </div>

 <div class="row mb-3 justify-content-center">
    <%= form.label :remaining_quantity, "Stock", class: "col-auto col-form-label" %>
    <div class="col">
      <%= form.number_field :remaining_quantity, class: "form-control" %>
    </div>
  </div>

 <div class="row mb-3">
    <%= form.submit "Confirmer", class:'btn btn-primary' %>
  </div>
<% end %>
