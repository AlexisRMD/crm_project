<!DOCTYPE html>
<html>
  <head>
    <title>CrmProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.2/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/boxicons@latest/css/boxicons.min.css">
  </head>
    <body id="body-pd">
      <header class="header" id="header">
          <div class="header_toggle"> <i class='bx bx-menu' id="header-toggle"></i> </div>
      </header>
      <div class="l-navbar" id="nav-bar">
          <nav class="nav">
              <div>
                <a href="#" class="nav_logo">
                  <i class='bx bx-layer nav_logo-icon'></i>
                  <span class="nav_logo-name">NoName Project</span>
                </a>
                <div class="nav_list"> 
                  <a href="/" class="nav_link <%= 'active' if request.path == '/' %>">
                    <i class='bx bx-grid-alt nav_icon'></i>
                    <span class="nav_name">Accueil</span>
                  </a>
                  <%= link_to(contacts_path, class:"nav_link #{'active' if request.path == contacts_path}") do %>
                    <i class='bx bx-user nav_icon'></i>
                    <span class="nav_name">Clients</span>
                  <% end %>
                  <%= link_to(products_path, class:"nav_link #{'active' if request.path == products_path}") do %>
                    <i class='bx bx-folder nav_icon'></i>
                    <span class="nav_name">Produits</span>
                  <% end %>
                </div>
              </div>
              <a href="#" class="nav_link">
                <i class='bx bx-log-out nav_icon'></i>
                <span class="nav_name">Déconnexion</span>
              </a>
          </nav>
      </div>
        <main>
          <div class="container text-center">
            <%= yield %>
            <p class="notice"><%= notice %></p>
            <p class="alert"><%= alert %></p>
          </div>
        </main>
      </div>
    </div>
  </body>
</html>
